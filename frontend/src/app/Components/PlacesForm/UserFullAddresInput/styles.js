import { makeStyles } from '@material-ui/core';

export const getStyles = makeStyles(() => ({
  root: {
    width: '100%',
  },
  toggleContainer: {
    width: '100%',
    display: 'flex',
    flexDirection: 'row',
  },
}));
